---
phase: 16-skill-reliability-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prompts/agent.system.main.tips.md
  - usr/skills/web-app-builder/SKILL.md
autonomous: true
requirements:
  - SKILL-01
  - SKILL-02
  - SKILL-03
  - SKILL-04

must_haves:
  truths:
    - "When the user asks to build/create any web app, the agent loads the web-app-builder skill before writing any code"
    - "The skill document specifies a numbered mandatory sequence: allocate port, copy template, customize, register, start, verify -- and explicitly states no step may be skipped"
    - "The skill document lists all reserved path names from app_proxy.py and specifies naming rules (lowercase, alphanumeric, hyphens only, 2-30 chars) -- with a validation command the agent must run before registering"
    - "The skill document includes a post-start health check that polls the app port via HTTP until it gets a response -- the agent must not declare success until the poll succeeds or times out with an error report"
  artifacts:
    - path: "prompts/agent.system.main.tips.md"
      provides: "System prompt instruction routing app creation requests to web-app-builder skill"
      contains: "web-app-builder"
    - path: "usr/skills/web-app-builder/SKILL.md"
      provides: "Complete skill document with mandatory sequence, validation, and health check"
      contains: "MANDATORY SEQUENCE"
  key_links:
    - from: "prompts/agent.system.main.tips.md"
      to: "usr/skills/web-app-builder/SKILL.md"
      via: "System prompt tells agent to load web-app-builder skill for any app creation request"
      pattern: "web-app-builder"
    - from: "usr/skills/web-app-builder/SKILL.md"
      to: "python/helpers/app_proxy.py"
      via: "Skill references the exact reserved paths defined in _RESERVED frozenset"
      pattern: "reserved"
    - from: "usr/skills/web-app-builder/SKILL.md"
      to: "python/api/webapp.py"
      via: "Skill uses webapp API for alloc_port, register, start actions"
      pattern: "localhost/webapp"
---

<objective>
Add system prompt routing awareness so the agent always recognizes app creation requests and uses the web-app-builder skill, then completely rewrite the skill document to enforce a mandatory bulletproof sequence with name validation and post-start health verification.

Purpose: Eliminate unreliable ad-hoc app creation by making the agent skill-aware at the system prompt level and making the skill itself enforce every step with validation gates.

Output: Updated system prompt tip + rewritten SKILL.md
</objective>

<execution_context>
@/Users/rgv250cc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rgv250cc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@prompts/agent.system.main.tips.md
@usr/skills/web-app-builder/SKILL.md
@python/helpers/app_proxy.py
@python/api/webapp.py
@apps/_templates/_GUIDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add app creation routing instruction to system prompt</name>
  <files>prompts/agent.system.main.tips.md</files>
  <action>
Add a new section to `prompts/agent.system.main.tips.md` (after the "Best practices" section) titled "## Apps System" with the following content:

```
## Apps System

when user asks to build create or deploy any web app dashboard visualization tool or browser-based interface:
1. always use skills_tool:load to load the web-app-builder skill first
2. follow the skill's mandatory sequence exactly — never write ad-hoc Flask/Python scripts outside the Apps System
3. apps are served at localhost:50000/{app_name}/ via the built-in proxy
```

This ensures the agent always routes app creation requests through the skill rather than improvising. The instruction style matches the existing terse prompt format in this file (no full sentences, imperative, compressed).
  </action>
  <verify>
Read `prompts/agent.system.main.tips.md` and confirm:
1. The "## Apps System" section exists after "## Best practices"
2. It references `web-app-builder` skill by name
3. It instructs using `skills_tool:load` before any app creation
4. It explicitly prohibits ad-hoc Flask scripts outside the Apps System
  </verify>
  <done>The system prompt contains an explicit instruction that directs the agent to load and follow the web-app-builder skill for any app/dashboard/visualization creation request</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite SKILL.md with mandatory sequence, name validation, and health check</name>
  <files>usr/skills/web-app-builder/SKILL.md</files>
  <action>
Completely rewrite `usr/skills/web-app-builder/SKILL.md`. Keep the existing YAML frontmatter (name, description, version, tags, trigger_patterns) but bump version to `3.0.0`. Replace ALL content below the frontmatter with the new structure below.

The new SKILL.md must have these sections in this exact order:

**1. Title + CRITICAL preamble**
```
# Web App Builder

CRITICAL: Follow the MANDATORY SEQUENCE below exactly. Do NOT skip steps. If any step fails, STOP and report the failure to the user — never proceed with a broken app.
```

**2. How Routing Works** (keep existing content, brief)
Explain: Browser -> localhost:50000/{app_name}/... -> proxy -> localhost:{PORT}/...

**3. MANDATORY SEQUENCE**
This is the core of the rewrite. Present as a numbered sequence with clear headers:

```
## MANDATORY SEQUENCE

Every app creation MUST follow these steps in order. No step may be skipped.

### Step 1 — Validate the app name

Before anything else, validate the chosen name:

**Rules:**
- Lowercase letters, digits, and hyphens only (regex: `^[a-z][a-z0-9-]{1,28}[a-z0-9]$`)
- Minimum 2 characters, maximum 30 characters
- Must start with a letter, must end with a letter or digit
- No underscores (use hyphens instead)

**Proxy-reserved names (blocked by `app_proxy.py` `_RESERVED`) — MUST NOT be used:**
`login`, `logout`, `health`, `dev-ping`, `socket.io`, `static`, `message`, `poll`, `settings_get`, `settings_set`, `csrf_token`, `chat_create`, `chat_load`, `upload`, `webapp`, `mcp`, `a2a`

**Built-in app names (occupied by core apps) — also avoid:**
`shared-browser`, `shared-terminal`

**Validation command (run this before proceeding):**
```bash
APP_NAME="chosen-name"
# Check format
echo "$APP_NAME" | grep -qP '^[a-z][a-z0-9-]{0,28}[a-z0-9]$' && echo "VALID" || echo "INVALID: must be lowercase alphanumeric + hyphens, 2-30 chars, start with letter"
# Check not reserved or occupied
BLOCKED="login logout health dev-ping socket.io static message poll settings_get settings_set csrf_token chat_create chat_load upload webapp mcp a2a shared-browser shared-terminal"
echo "$BLOCKED" | tr ' ' '\n' | grep -qx "$APP_NAME" && echo "BLOCKED: choose a different name" || echo "NAME AVAILABLE"
```

If the name is INVALID or RESERVED, tell the user and ask for a different name. Do NOT proceed.
```

```
### Step 2 — Choose a template

Read `/a0/apps/_templates/_GUIDE.md` for the decision tree. Quick reference:

| Need | Template | Start command |
|------|----------|---------------|
| Charts, metrics, live data | `flask-dashboard` | `python app.py` |
| Web app with Python logic | `flask-basic` | `python app.py` |
| Pure front-end, no Python | `static-html` | `python serve.py` |

Tell the user which template you chose and why.
```

```
### Step 3 — Allocate a port

```bash
PORT=$(curl -s "http://localhost/webapp?action=alloc_port" | python3 -c "import sys,json; print(json.load(sys.stdin)['port'])")
echo "Allocated port: $PORT"
```

If this fails or returns empty, STOP and report the error.
```

```
### Step 4 — Copy the template

```bash
cp -r /a0/apps/_templates/{TEMPLATE} /a0/apps/{APP_NAME}
```

Verify the copy succeeded:
```bash
ls /a0/apps/{APP_NAME}/app.py || ls /a0/apps/{APP_NAME}/serve.py
```

If the directory doesn't exist or is empty, STOP and report the error.
```

```
### Step 5 — Customize the app

Edit the copied files to implement what the user requested. Key rules that MUST NOT change:
- Always `host="0.0.0.0"` (not 127.0.0.1)
- Always read port from env: `PORT = int(os.environ.get("PORT", 9000))`
- Always read app name from env: `APP_NAME = os.environ.get("APP_NAME", "")`
- Always use relative asset URLs (the `<base>` tag handles sub-path routing)
- Never hardcode port numbers
```

```
### Step 6 — Register the app

```bash
curl -s -X POST http://localhost/webapp \
  -H "Content-Type: application/json" \
  -d "{\"action\":\"register\",\"name\":\"{APP_NAME}\",\"port\":$PORT,\"cmd\":\"{START_CMD}\",\"cwd\":\"/a0/apps/{APP_NAME}\",\"description\":\"{DESCRIPTION}\"}"
```

Check the response for `"error"` — if present, STOP and report it.
```

```
### Step 7 — Start the app

```bash
curl -s -X POST http://localhost/webapp \
  -H "Content-Type: application/json" \
  -d "{\"action\":\"start\",\"name\":\"{APP_NAME}\"}"
```

Check the response for `"error"` — if present, STOP and report it.
```

```
### Step 8 — Verify the app is running (REQUIRED)

Poll the app's port until it responds. Do NOT skip this step.

```bash
# Wait up to 10 seconds for the app to start responding
for i in $(seq 1 20); do
  if curl -sf -o /dev/null "http://127.0.0.1:$PORT/"; then
    echo "HEALTHY: App is responding on port $PORT"
    break
  fi
  if [ "$i" -eq 20 ]; then
    echo "FAILED: App not responding after 10 seconds"
    echo "Check logs or process status:"
    echo "  ps aux | grep '$APP_NAME'"
    echo "  curl -v http://127.0.0.1:$PORT/"
  fi
  sleep 0.5
done
```

- If HEALTHY: Tell the user the app is live at `localhost:50000/{APP_NAME}/` and open it with `open_app`.
- If FAILED: Tell the user the app failed to start. Check for Python errors, port conflicts, or missing dependencies. Do NOT say "your app is ready".
```

**4. Management Commands** (keep existing content, brief reference)
List, stop, restart, autostart, remove commands.

**5. Troubleshooting** (keep existing table, updated)

**6. Template Customization Quick Reference** (brief per-template guidance for Step 5)

The tone should be imperative and direct. Every step that can fail must have an explicit "if this fails, STOP" instruction. The word "MANDATORY" must appear. The health check poll loop must be copy-pasteable bash.

Do NOT include underscores in app name examples (the new naming rules disallow them). Use hyphens instead. Update all examples: `my_dashboard` -> `my-dashboard`, `my_app` -> `my-app`, `sales_viz` -> `sales-viz`, `log_viewer` -> `log-viewer`, `data_explorer` -> `data-explorer`.
  </action>
  <verify>
Read the new `usr/skills/web-app-builder/SKILL.md` and confirm ALL of the following:
1. Version is 3.0.0 in frontmatter
2. CRITICAL preamble exists stating no steps may be skipped
3. Step 1 validates app name with regex `^[a-z][a-z0-9-]{1,28}[a-z0-9]$`
4. Step 1 lists ALL proxy-reserved names from `python/helpers/app_proxy.py` `_RESERVED` frozenset (excluding empty string) PLUS built-in app names (shared-browser, shared-terminal) as a separate "occupied" group
5. Step 1 includes a runnable bash validation command
6. Steps 2-7 cover: choose template, allocate port, copy template, customize, register, start
7. Step 8 has a health check poll loop (curl in a for loop with sleep 0.5, max 10s)
8. Step 8 has explicit HEALTHY/FAILED branches with different user-facing messages
9. Every step that can fail has "STOP and report" language
10. No underscore-based app name examples remain (all use hyphens)
11. The word "MANDATORY" appears in the document
  </verify>
  <done>SKILL.md enforces a complete mandatory 8-step sequence where: (a) name is validated against reserved paths and naming rules before any work begins, (b) each step has explicit failure handling, and (c) the app is health-checked via HTTP poll before the agent declares success</done>
</task>

</tasks>

<verification>
1. Read `prompts/agent.system.main.tips.md` — contains "## Apps System" section referencing web-app-builder skill
2. Read `usr/skills/web-app-builder/SKILL.md` — version 3.0.0, contains MANDATORY SEQUENCE with 8 steps
3. Verify Step 1 proxy-reserved names match `python/helpers/app_proxy.py` `_RESERVED` (minus empty string): login, logout, health, dev-ping, socket.io, static, message, poll, settings_get, settings_set, csrf_token, chat_create, chat_load, upload, webapp, mcp, a2a — PLUS built-in app names (shared-browser, shared-terminal) listed separately as "occupied"
4. Verify Step 8 health check uses `curl -sf` in a loop with timeout
5. Verify no underscore app names in examples
6. Verify every step has failure/STOP language
</verification>

<success_criteria>
- System prompt tips include explicit routing instruction for app creation -> web-app-builder skill
- SKILL.md version 3.0.0 with mandatory 8-step sequence
- Name validation with regex + reserved name check + runnable bash command
- Post-start HTTP health poll with HEALTHY/FAILED branches
- All steps have explicit "STOP and report" failure handling
- No ad-hoc app creation possible if agent follows the prompt + skill
</success_criteria>

<output>
After completion, create `.planning/phases/16-skill-reliability-core/16-01-SUMMARY.md`
</output>
