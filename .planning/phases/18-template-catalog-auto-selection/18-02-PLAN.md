---
phase: 18-template-catalog-auto-selection
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - usr/skills/web-app-builder/SKILL.md
autonomous: true
requirements: [SKILL-05]

must_haves:
  truths:
    - "Agent reads the template catalog and auto-selects the best-matching template for the user's request"
    - "Agent tells the user which template was chosen and why before proceeding with app creation"
    - "User can override the template selection by asking for a different one without restarting"
  artifacts:
    - path: "usr/skills/web-app-builder/SKILL.md"
      provides: "Auto-selection logic in Step 2 with keyword matching, user notification, and override handling"
      contains: "auto-select"
  key_links:
    - from: "usr/skills/web-app-builder/SKILL.md"
      to: "apps/_templates/_CATALOG.md"
      via: "Step 2 instruction to read catalog"
      pattern: "_CATALOG\\.md"
    - from: "usr/skills/web-app-builder/SKILL.md"
      to: "apps/_templates/_GUIDE.md"
      via: "Step 2 instruction to read guide"
      pattern: "_GUIDE\\.md"
---

<objective>
Update the web-app-builder SKILL.md Step 2 to include auto-selection logic — the agent matches the user's request to the best template, tells the user the selection, and handles overrides.

Purpose: Currently Step 2 tells the agent to "Read _GUIDE.md" and shows a 3-template table. This needs to become an intelligent selection step that uses the new _CATALOG.md, covers all 7 templates, communicates the choice, and allows the user to switch.

Output: Updated `usr/skills/web-app-builder/SKILL.md` with auto-selection in Step 2
</objective>

<execution_context>
@/Users/rgv250cc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rgv250cc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@usr/skills/web-app-builder/SKILL.md
@.planning/phases/18-template-catalog-auto-selection/18-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add auto-selection logic to SKILL.md Step 2</name>
  <files>usr/skills/web-app-builder/SKILL.md</files>
  <action>
Rewrite Step 2 ("Choose a template") of `usr/skills/web-app-builder/SKILL.md` to add auto-selection, user notification, and override handling. Keep all other steps (1, 3-8) UNCHANGED.

**Replace Step 2 with the following structure:**

### Step 2 — Auto-select a template

1. **Read the template catalog:** Read `/a0/apps/_templates/_CATALOG.md` for the full list of templates with descriptions, use cases, and selection criteria.

2. **Match the user's request to a template** using this priority order:
   - If the user explicitly names a template (e.g., "use the crud-app template"), use that template
   - Otherwise, match based on keywords in the user's request:

   | Keywords / signals in request | Template | Start command |
   |-------------------------------|----------|---------------|
   | "database", "CRUD", "records", "items", "manage data", "list/edit/delete" | `crud-app` | `python app.py` |
   | "real-time", "live", "streaming", "SSE", "multiple charts" | `dashboard-realtime` | `python app.py` |
   | "dashboard", "metrics", "monitoring", "charts" | `flask-dashboard` | `python app.py` |
   | "upload", "download", "files", "convert", "file manager" | `file-tool` | `python app.py` |
   | "calculator", "converter", "text tool", "utility", "simple tool" | `utility-spa` | `python serve.py` |
   | "visualization", "D3", "Plotly", "static page", "no backend" | `static-html` | `python serve.py` |
   | General web app, forms, pages, custom API, or unclear | `flask-basic` | `python app.py` |

   If the request is ambiguous, default to `flask-basic` (most flexible).

3. **Tell the user your selection:** Before proceeding to Step 3, always say something like:
   > "I'll use the **{template-name}** template for this — it's the best fit because {brief reason}. If you'd prefer a different template, just let me know."

4. **Handle override:** If the user asks to use a different template (now or later):
   - Acknowledge the change
   - Switch to the requested template
   - Continue from Step 3 (allocate port) — do NOT restart from Step 1

Also update the "Template customization quick reference" section at the bottom of SKILL.md to include entries for the 4 new templates:

**`dashboard-realtime`** — live streaming dashboard:
- Edit `generate_data()` in `app.py` to produce your metrics/chart data
- SSE streams every 2s; adjust interval in the generator loop
- Three chart types available: line, bar, doughnut

**`utility-spa`** — lightweight single-page tool:
- Replace transform functions in `app.js` with your tool logic
- Input/output layout: textarea input, action buttons, output area
- Start command is `python serve.py` (not `app.py`)
- No Python backend — serve.py only serves static files

**`crud-app`** — data management app:
- Edit the Item model section in `app.py` to define your fields and CREATE_TABLE_SQL
- Routes handle list/detail/create/edit/delete automatically based on the model
- SQLite database auto-creates on first request

**`file-tool`** — file upload and conversion:
- Edit ALLOWED_EXTENSIONS and MAX_CONTENT_LENGTH in `app.py` for your needs
- Add conversions: extend CONVERSIONS dict in `app.py` + CONVERT_OPTIONS in `app.js`
- Uploads stored in `uploads/` directory (auto-created)

Do NOT change the version number (keep 3.0.0). Do NOT modify Steps 1, 3, 4, 5, 6, 7, or 8.
  </action>
  <verify>
Verify auto-selection elements are present in SKILL.md:
```bash
grep -q "_CATALOG.md" usr/skills/web-app-builder/SKILL.md && echo "Catalog ref: YES" || echo "Catalog ref: NO"
grep -q "auto-select\|Auto-select" usr/skills/web-app-builder/SKILL.md && echo "Auto-select: YES" || echo "Auto-select: NO"
grep -q "override\|Override" usr/skills/web-app-builder/SKILL.md && echo "Override: YES" || echo "Override: NO"
grep -q "crud-app" usr/skills/web-app-builder/SKILL.md && echo "crud-app: YES" || echo "crud-app: NO"
grep -q "dashboard-realtime" usr/skills/web-app-builder/SKILL.md && echo "dashboard-realtime: YES" || echo "dashboard-realtime: NO"
grep -q "file-tool" usr/skills/web-app-builder/SKILL.md && echo "file-tool: YES" || echo "file-tool: NO"
grep -q "utility-spa" usr/skills/web-app-builder/SKILL.md && echo "utility-spa: YES" || echo "utility-spa: NO"
# Verify other steps are untouched
grep -q "Step 1 — Validate the app name" usr/skills/web-app-builder/SKILL.md && echo "Step 1 intact: YES" || echo "Step 1 intact: NO"
grep -q "Step 8 — Verify the app is running" usr/skills/web-app-builder/SKILL.md && echo "Step 8 intact: YES" || echo "Step 8 intact: NO"
```
  </verify>
  <done>SKILL.md Step 2 includes: (1) instruction to read _CATALOG.md, (2) keyword-based template matching table with all 7 templates, (3) mandatory user notification of template choice with reason, (4) override handling that allows switching without restart. Template customization reference includes all 7 templates. Steps 1 and 3-8 are unchanged.</done>
</task>

</tasks>

<verification>
1. SKILL.md Step 2 references `_CATALOG.md` for template discovery
2. All 7 templates appear in the keyword matching table
3. User notification instruction is present ("Tell the user your selection")
4. Override handling is documented (switch template without restart)
5. Steps 1, 3, 4, 5, 6, 7, 8 remain unchanged from v3.0.0
6. Template customization reference at bottom covers all 7 templates
</verification>

<success_criteria>
- SKILL.md Step 2 directs the agent to read the catalog, match keywords, select a template, notify the user, and handle overrides
- All 7 templates are in the matching table with appropriate keywords and start commands
- The agent's selection is always communicated to the user before proceeding
- User can override the selection at any point without restarting the workflow
- No other steps in the MANDATORY SEQUENCE were modified
</success_criteria>

<output>
After completion, create `.planning/phases/18-template-catalog-auto-selection/18-02-SUMMARY.md`
</output>
