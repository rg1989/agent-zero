# Flask is pre-installed in the Agent Zero environment.
# No additional packages are required for this template.
#
# If your app needs extra packages, add them here and run:
#   pip install -r requirements.txt
